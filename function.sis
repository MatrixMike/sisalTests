define polar
global sqrt( x: real returns real );
global atan( x: real returns real );
function polar (x,y: real; returns real)
    let 
      r := sqrt (x*x + y*y);
      theta := atan(y/x);
    in
      r 
    end let
end function
define main


